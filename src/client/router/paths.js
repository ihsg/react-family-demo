export const root = '/';
export const user = '/user';
export const userLogin = `${user}/login`;
export const userRegister = `${user}/register`;

export default {
  root,
  user,
  userLogin,
  userRegister,
};
